apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: demo-flask
  labels:
    app.kubernetes.io/name: demo-flask
    app.kubernetes.io/part-of: demo-flask-project
    kubernetes.azure.com/generator: devhub
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: demo-flask